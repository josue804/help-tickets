import { timeSubmitted } from '../../../helpers/time-submitted';
import { module, test } from 'qunit';

module('Unit | Helper | time submitted');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = timeSubmitted(42);
  assert.ok(result);
});
